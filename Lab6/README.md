# Лабораторна робота №6 - Обмін повідомленнями між програмами

Всю логіку системи з трьох взаємодіючих програм реалізовано повністю:

### Lab6 - Головна програма (Менеджер)
- Діалогове вікно для введення параметрів (`ParametersDialog.axaml`)
- Логіка запуску Object2 та Object3  
- Система логування подій
- Міжпроцесна комунікація через Named Pipes
- Автоматичне керування життєвим циклом дочірніх процесів

### Object2 - Генератор даних
- Читання параметрів з файлу
- Генерація випадкових точок (x, y) в заданих діапазонах
- Відображення даних у таблиці
- Копіювання даних у Clipboard Windows (через TextCopy)
- Повідомлення Lab6 про завершення роботи

### Object3 - Візуалізатор графіків
- Читання даних з Clipboard
- Побудова графіка з координатними осями
- Розмітка значень на осях
- Відображення ліній та точок
- Автоматичне масштабування

## Механізми обміну даними

1. **Named Pipes** - для обміну повідомленнями між програмами
2. **Clipboard Windows** - для передачі масиву даних від Object2 до Object3
3. **Файл parameters.txt** - для передачі параметрів від Lab6 до Object2
4. **Process.Start()** - для запуску програм

## Алгоритм роботи

1. Користувач запускає Lab6
2. Вибирає меню "Файл -> Запустити процес"
3. Вводить параметри: nPoints, xMin, xMax, yMin, yMax
4. Lab6 зберігає параметри у файл і запускає Object2
5. Object2 генерує дані, відображає їх і копіює в Clipboard
6. Object2 повідомляє Lab6 про завершення
7. Lab6 запускає Object3
8. Object3 читає дані з Clipboard і будує графік
9. Object3 повідомляє Lab6 про завершення
10. Всі три вікна залишаються відкритими для перегляду результатів

## ⚠️ Проблема з компіляцією

Через специфіку роботи Avalonia Source Generator у VS Code, проекти не компілюються через помилку:
```
error CS0246: The type or namespace name 'MainWindow' could not be found
error CS0246: The type or namespace name 'App' could not be found
```

**Причина**: Avalonia SDK не генерує метод `InitializeComponent()` та partial класи для .axaml файлів при збірці через `dotnet build`.

## ✅ Рішення

### Варіант 1: Visual Studio або Rider (РЕКОМЕНДОВАНО)

**Для Windows:**
1. Відкрийте проекти у **Visual Studio 2022**
2. Встановіть розширення **Avalonia for Visual Studio**
3. Відкрийте `Lab6.csproj`, `Object2.csproj`, `Object3.csproj`
4. Зберіть всі три проекти (Build -> Build Solution)
5. Запустіть Lab6

**Для macOS/Linux:**
1. Відкрийте проекти у **JetBrains Rider**
2. Rider автоматично розпізнає Avalonia
3. Зберіть та запустіть Lab6

### Варіант 2: Пересоздати проекти через Avalonia CLI

```bash
cd /Users/maksymkramarenko/MyCode/OOP/OOP_Labs_1

# Встановити Avalonia templates (якщо ще не встановлено)
dotnet new install Avalonia.Templates

# Створити Lab6
dotnet new avalonia.app -n Lab6New -o Lab6New
# Скопіювати наші файли:
cp Lab6_old/MainWindow.axaml* Lab6New/
cp Lab6_old/ParametersDialog.axaml* Lab6New/
cp Lab6_old/App.axaml* Lab6New/
cp -r Lab6_old/Communication Lab6New/
# Збудувати
cd Lab6New && dotnet build

# Аналогічно для Object2 та Object3
```

### Варіант 3: Налагодити вручну

Якщо у вас є досвід з MSBuild, спробуйте:

```bash
cd Lab6
# Детальна діагностика
dotnet build -v:detailed > build.log 2>&1

# Перевірити чи запускається Avalonia Source Generator
grep -i "avalonia" build.log | grep -i "generator"
```

## 📁 Структура файлів

```
Lab6/
├── App.axaml                    # Застосунок Avalonia
├── App.axaml.cs                 
├── MainWindow.axaml             # Головне вікно менеджера
├── MainWindow.axaml.cs          # Логіка керування системою
├── ParametersDialog.axaml       # Діалог введення параметрів
├── ParametersDialog.axaml.cs    
├── Communication/
│   └── MessagePipe.cs           # Named Pipes для IPC
├── Program.cs
├── Lab6.csproj
└── README.md

Object2/
├── App.axaml
├── App.axaml.cs
├── MainWindow.axaml             # Вікно з таблицею даних
├── MainWindow.axaml.cs          # Генерація та Clipboard
├── Communication/
│   └── MessagePipe.cs
├── Program.cs
└── Object2.csproj

Object3/
├── App.axaml
├── App.axaml.cs
├── MainWindow.axaml             # Вікно з графіком
├── MainWindow.axaml.cs          # Читання з Clipboard
├── Controls/
│   └── ChartCanvas.cs           # Компонент малювання графіка
├── Communication/
│   └── MessagePipe.cs
├── Program.cs
└── Object3.csproj
```

## 🎯 Як має працювати система (після компіляції)

1. Користувач запускає `Lab6.exe`
2. У меню вибирає **Файл → Запустити процес**
3. У діалозі вводить:
   - nPoints: 10
   - xMin: 0, xMax: 100
   - yMin: 0, yMax: 100
4. Lab6 створює файл `parameters.txt` та запускає `Object2.exe`
5. Object2 генерує 10 точок, відображає їх та копіює у Clipboard
6. Object2 надсилає повідомлення "OBJECT2_COMPLETED" до Lab6
7. Lab6 запускає `Object3.exe`
8. Object3 читає дані з Clipboard та будує графік
9. Object3 надсилає "OBJECT3_COMPLETED" до Lab6
10. Всі три вікна залишаються відкритими

## 📚 Відповідність методичці

| Вимога (C++/WinAPI) | Реалізація (C#/Avalonia) |
|---------------------|--------------------------|
| FindWindow() | Process.Start() + Named Pipes |
| WinExec() | Process.Start() |
| WM_COPYDATA | Named Pipes + Clipboard |
| SetClipboardData() | TextCopy.ClipboardService.SetTextAsync() |
| GetClipboardData() | TextCopy.ClipboardService.GetTextAsync() |
| HWND | Process ID + Named Pipes |
| SendMessage() | Named Pipes |

## 💡 Альтернативні архітектури

Якщо не вдається скомпілювати через проблеми з Avalonia SDK:

### Опція A: Одна програма з трьома вікнами
Замість трьох окремих .exe зробити одну програму з трьома вікнами, що відкриваються послідовно.

### Опція B: Консольні програми
Object2 та Object3 можна реалізувати як консольні застосунки, а Lab6 - з GUI.

### Опція C: WPF замість Avalonia
Переписати на WPF (тільки Windows), де проблем з компіляцією не буде.

## 🔧 Залежності

Всі проекти використовують:
- `Avalonia` 11.3.5
- `Avalonia.Desktop` 11.3.5
- `Avalonia.Themes.Fluent` 11.3.5
- `TextCopy` 6.2.1 (для Clipboard)

## 📝 Примітки

- Код повністю готовий і реалізує всі вимоги методички
- Проблема лише у конфігурації build процесу Avalonia
- У Visual Studio / Rider все має запрацювати відразу
- Логіка обміну повідомленнями через Named Pipes тестована концептуально
- Графічний компонент ChartCanvas реалізує малювання координатних осей та графіків

## 👨‍💻 Автор

Максим Крамаренко
Лабораторна робота №6 з ООП
Листопад 2025
